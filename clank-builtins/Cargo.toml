[package]
name = "clank-builtins"
version = "0.1.0"
edition = "2021"

[dependencies]
brush-core = "0.4.0"
clap = { version = "4", features = ["derive"] }
async-trait = "0.1"
thiserror = "2"
tokio = { version = "1", features = ["full"] }

[dev-dependencies]
clank = { path = "../clank" }
